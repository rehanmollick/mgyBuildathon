"""Agent modules: one file per agent, all plain functional modules.

Each agent exposes a single top-level function with a typed signature.
No base classes, no framework. See ``docs/adr/001-multi-agent-architecture.md``.
"""
